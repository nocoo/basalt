# 2.0 计划：可发布控件库 + 展示站

> 状态：与 02 联审。P0 已 Sign Off（基线 `35fdfaa`）。未完成第 13 节拍板前不进入 Wave B / Wave P / 阶段 0。
> 当前版本：`package.json` → `1.3.5`。仓库**没有** `v1.3.5` tag。视觉基线 SHA = **B0**（播种后的代码 HEAD）。B1 只存截图并把该 SHA 写入 §3.2。必须在 Wave P 改侧栏之前取样。
> 参考：Kumo commit `1159868dfe32`，仓路径 `/Users/nocoo/workspace/references/kumo`（MIT）。

架构与出口表以本文为准。**怎么提交、什么顺序、图表 kit、底稿确认门**见 [02-implementation.md](./02-implementation.md)。

---

## 0. 本轮修订相对初稿改了什么

| 初稿 / 第一轮问题 | 修订 |
|----------|------|
| 「74 个控件」不可核验 | 第 6 节拆成两张表：覆盖映射 + **最终公开出口（一名一行）** |
| CSS 只改了变量名仍会污染 utility | 包内 class 必须 `bg-basalt-*` / `text-basalt-*` / `rounded-basalt-*`；**不**再注册 `--color-background`。双契约分开验收 |
| 「Gen 1 = 非浮岛」与源码不符 | 本站 **已经有内容区浮岛**（`DashboardLayout` 内 `rounded-[16px] md:rounded-[20px] bg-card`）。Gen 1 指 **DashboardLayout + AppSidebar 结构**，不是「没有岛」 |
| 「L2 一律无边」会改现页 | 现页卡面不统一：`ActionGridCard` 无边，`BulletChartCard` 是 `border-border bg-card`。冻结 **逐实例现状**，不统一改皮肤 |
| 6.2「12 个现页在用」不实 | 按调用核验。未使用的 ui 标 `catalog`，不标「现页必需」 |
| 先做完所有控件再接线 | 壳仍最后换。**stable/chart/provider**：文档页 `/ui/:name` + 6.2 现页 wire。**catalog**：只需 `/ui/:name` |
| 发布面只写了 peer 一句话 | 第 5.3 节补 exports、dts、依赖归属、tarball 门、图表不进根 barrel |
| Maps / RefreshButton 等口径混乱 | manifest 里写 merge / exclude |
| G2「不把 token 写进包」 | 改为「不把密钥写进包」；CSS tokens 必须随包 |

---

## 1. 为什么做 2.0

Basalt 现在是 private Vite SPA（无 `exports`）。视觉语言被家族大量复制，没有共享代码。

| 现象 | 证据 |
|------|------|
| 本机产品 UI 复制皮肤 | `ai-arsenal` `backy` `bat` `dove` `fundly` `gecko` `lyre` `meowth` `pew` `raven` `surety` `xray` `zhe` 等有 `L0 body` 或 `ui/button.tsx` |
| 控件文件重复 | `personal/` 内约 308 个 `components/ui/*.tsx`；`ui/button.tsx` 15 份、15 个 hash |
| 壳分叉 | `app-shell.tsx` 14 份 14 个 hash。本仓库是 `DashboardLayout.tsx`，没有 `app-shell.tsx` |
| 新 clone 仍在用 | `otter` `pika` `ellie` `noheir` `signoff.now` `steed` `arena` 等 |
| 零跨仓依赖 | 无项目 import 公共 Basalt |

家族文档称本仓库为 **Gen 1 源**，surety / meowth 为 **Gen 2 活样本**（四层亮度、三文件壳、焦点归还）。见 `meowth/docs/features/02-dashboard-redesign-to-basalt-gen2.md`。

**不要把「Gen」当成一种皮肤。** 能力拆开写在第 12 节。本站现状：内容区已经是浮岛；侧栏仍是 Gen 1 单体；亮度注释是 L0/L1/L2，不是 L0–L3。

2.0：本仓库变成库的第一消费者，再发 `@nocoo/basalt`，其它项目以后再替换拷贝。

---

## 2. 目标与非目标

### 2.1 目标

1. npm 包 `@nocoo/basalt`：ESM、granular export、可 tree-shake、CSS 可被 Vite 与 Next 消费。
2. 本仓库同时是展示站：控件文档 + 现有组合页。
3. **已有路由**在批准基线 commit 上的观感冻结（见 3.2）。
4. Kumo 有的职责，manifest 里必须有对应行（实现、合并或明确排除）。
5. 实现时从家族挑 API 底稿，视觉以本站基线为准。
6. 先控件后壳；每个控件完成即接线，不是攒到最后。

### 2.2 非目标（2.0 不做）

- 不迁其它产品仓。
- 不把 Radix 换成 Base UI。
- 不上 Figma、视觉回归 Worker、CLI `basalt add`。
- 不把本站壳改成 meowth 三文件 `app-shell` / `sidebar-context`。
- 不把 shadcn 薄包装直接当稳定 API。
- 不把 TanStack DataTable、ActionGrid、登录工牌当 2.0 稳定控件（当 block 或留 app）。
- 不做地图控件（Kumo `BubbleMap` / `ChoroplethMap` 明确 **exclude**）。

---

## 3. 原则

### 3.1 站 = 控件文档 + 组合展示

| 层 | 2.0 | 现在 |
|----|-----|------|
| 控件文档 | **每个最终公开出口**一条 `/ui/:name` 页。侧栏只挂分类索引，不把出口平铺进顶层 nav | `/components` 等不能 copy |
| 组合展示 | 现有 dashboard / accounts / 场景 / login，逐步改 import 包 | 写死 mock 的 `*Card` |

侧栏视觉与分组容器继续用 `AppSidebar.tsx`。文档入口**不**给每个控件一条顶层 nav。改为：

- 现有分组不动（含已有 `nav.controls`「控件」：Interactive / Data / Forms / Navigation）
- 新增一组 **「控件库」**（`nav.kit`），默认折叠，里面是分类索引（动作 / 表单 / 浮层 / 图表…），点进 `/ui/:name`。禁止复用现有「控件」组名，避免两组都叫「控件」
- ⌘K 能搜到控件文档

新增分组是已有壳上**唯一允许的导航变化**（第 13 节拍板）。

### 3.2 观感冻结（可执行）

**基线 commit：** **B0 HEAD**（播种后的代码）。B1 是截图 commit，不是代码基线；由 **B1 唯一**把 B0 SHA 写入本节替换本句。阶段 0 **不再**重钉 SHA。批准拍板时的 HEAD 若仍含未播种随机数，**不得**直接当基线。没有 `v1.3.5` tag，不要写「对照 1.3.5 截图」除非先打 tag 或存截图。禁止用浏览器 `addInitScript` 偷换 `Math.random` 来「冻」与源码不一致的图。

**Wave B 产出冻结夹（在 Wave P 改侧栏之前）：** `docs/baselines/2-0/`（只存图，不是控件 fixture）。矩阵：已有路由 × `{light,dark}` × desktop `1280×800` × mobile `390×844`。浏览器：本机 Chrome。locale `zh-CN`，timezone `Asia/Shanghai`，`prefers-reduced-motion: reduce`。现网未播种点（至少 `SummaryMetricCard.tsx`、`SecondaryMetricCard.tsx`、`src/data/mock.ts` 的 `performanceData`）必须在 B0 换成播种 PRNG 或固定数组后再拍。另存：侧栏展开/折叠、移动抽屉开、一个 Dialog 开。容差：阶段 7 人工并排，2.0 不做像素 diff 门。Wave P 之后允许侧栏多一个默认折叠的「控件库」组，不重拍整表。

**冻什么：** 已有路由里，每个现成实例的布局、颜色、圆角、字号、图表形状、边框有无。

**不冻成「设计规范理想态」：**

- 禁止把全部卡改成无边 L2。`BulletChartCard` 现在就是 `border-border bg-card`（`src/components/dashboard/BulletChartCard.tsx`）。
- 禁止把 Button 默认从本站 `h-9` 改成 meowth `h-10`。
- 禁止把 Input 默认从本站 `bg-background rounded-md` 改成另一种表面，除非该实例本来就被页面 `className` 覆盖——覆盖的跟覆盖走，默认跟默认走。
- 本站内容区浮岛（`DashboardLayout.tsx` 约 122–123 行）**属于基线，要保留**。禁止改成 meowth `rounded-island` 的壳结构。

**允许：**

- 新文档路由
- 侧栏多一个默认折叠的「控件库」组（若第 13 节同意）
- DOM 因 compound 变化，像素一致

**每个现页替换时的规则：** 该页现在用什么 class，替换后计算样式仍是那套。控件默认值可以是「规范表面」，但现页必须通过 `className` / variant **复现原样**，而不是把现页拽去规范。

### 3.3 发 npm 包

包名 `@nocoo/basalt`。

Tailwind 消费方（与 Kumo 相同，顺序硬性）：

```css
@source "../node_modules/@nocoo/basalt/dist/**/*.{js,jsx,ts,tsx}";
@import "@nocoo/basalt/styles/tailwind";
@import "tailwindcss";
```

非 Tailwind：

```ts
import "@nocoo/basalt/styles/standalone";
```

组件：

```ts
import { Button } from "@nocoo/basalt/components/button";
```

根 barrel **不含图表**，避免 `import { Button }` 拉到 recharts。图表走 `@nocoo/basalt/charts/donut`。

本仓库 workspace 自吃。开发期可 alias 到源码，但 **发布门必须用打包 tarball 在仓外 tmp 跑 A/B/C/D**（见 5.3）。Alias 不能代替门。

### 3.4 对齐 Kumo 的面，用我们的皮

可参考：控件切分、compound、可机器读 variants、文档页结构、components vs blocks。

不可抄：`bg-kumo-*`、Phosphor、Base UI 视觉、Kumo 侧栏 IA、ECharts。

Kumo `CloudflareLogo` / `PoweredByCloudflare` → `BasaltMark`（本站 Mountain）。不实现 Cloudflare 标。

若源码级参考 Kumo，发布包 README 保留 Cloudflare MIT notice。

### 3.5 顺序

```
Wave B：B0 播种未播种随机数据 + B1 冻结截图（必须在改侧栏之前，见 02）
  → Wave P：全部 `/ui/:name` placeholder + 「控件库」导航（见 02）
    → 阶段 0：workspace + token/CSS 契约
      → 逐个控件：实现 + L1 测试 + `/ui/:name`；stable/chart/provider 再接到 6.2 现页 wire
        → 稳定清单清零
          → 拼回壳与剩余组合页
            → tarball 门通过
              → 先发 2.0.0-alpha，自吃后再 2.0.0
```

执行顺序、提交粒度听 02。Sidebar 现页 `wire=/` 推迟到阶段 7，见 6.2 注。

壳仍最后换。叶子控件不准「做完只躺在 packages 里」。

---

## 4. 分层

| 层 | 含义 | 2.0 |
|----|------|-----|
| Tokens | `--basalt-*`，showcase 映射回现有 `--background` 等，现页 utility 不变 | 做 |
| Components | 无业务数据、无 i18n、无 `window` 在模块顶层 | 做，见 manifest `stable` / `catalog` |
| Blocks | 有布局/产品假设 | 2.0 不发 CLI；DataTable、ActionGrid、PageIntro、登录工牌留 app 或标 block |
| Showcase | 文档 + 组合页 | 做 |

Theme 需要 `ThemeProvider`。`ThemeToggle` 不是孤立按钮：包内提供 Provider（SSR 用 `useSyncExternalStore` / 注入），禁止在模块顶层读 `localStorage`。

---

## 5. 仓库与发布面

### 5.1 目录

```
basalt/
  packages/basalt/                 # @nocoo/basalt
    package.json
    src/index.ts                   # 根 barrel：不含 charts
    src/styles/
      tokens.css                   # --basalt-* 与 light/dark
      tailwind.css                 # @theme 映射，供 Tailwind 消费方
    src/components/<name>/
    src/charts/<name>/             # 不进根 barrel
    src/providers/theme.tsx
    src/providers/link.tsx
  fixtures/
    vite-tailwind/                 # 模板，复制到仓外 tmp 跑门 A
    vite-standalone/               # 模板，复制到仓外 tmp 跑门 B
    next19/                        # 模板，复制到仓外 tmp 跑门 C（React 19）
  src/                             # 展示站
  docs/baselines/2-0/              # 冻结截图
```

granular 路径统一 `@nocoo/basalt/components/<name>`；图表 `@nocoo/basalt/charts/<name>`。不要混用两种「控件根路径」。

### 5.2 CSS 与主题

只抽 `:root` **不够**。还要字体、keyframes、chart/heatmap 变量。全部进 **basalt 命名空间**：

| 种类 | 包内允许 | 禁止 |
|------|----------|------|
| 色 / 边 / 圆角 | `--color-basalt-*`、`--radius-basalt-card`、`--radius-basalt-widget`、`--radius-basalt-lg/md/sm`；utility `bg-basalt-*` `text-basalt-*` `border-basalt-*` `rounded-basalt-card` `rounded-basalt-widget` | `--color-background`、`.bg-background`、无前缀 `rounded-card` |
| 字体 | `--font-basalt-sans`、`--font-basalt-display`、`font-basalt-sans`、`font-basalt-display` | 包内 `font-display` / `font-sans` 无前缀 |
| 动画 | `@keyframes basalt-accordion-down` 等、`animate-basalt-*` | 无前缀 `accordion-down`、裸用 `tw-animate-css` 全局名 |

包的 `@theme` **只**注册 `--color-basalt-*`、`--radius-basalt-*`、`--font-basalt-*`、`--animate-basalt-*`。G1 与后续 lint 按此执行。

Showcase 现页仍用 `bg-background`：只在 **showcase 自己的 CSS** 里做映射，不进包：

```css
@source "../packages/basalt/src/**/*.{ts,tsx}";
@import "@nocoo/basalt/styles/tailwind";
@import "tailwindcss";
:root {
  --background: var(--basalt-background);
  /* 仅 showcase：让旧页 bg-background 继续工作，直到被替换 */
}
```

顺序与 §3.3 相同（硬性）：`@source` → basalt styles → `tailwindcss`。basalt `@theme` 必须在 `@import "tailwindcss"` 之前注册。Showcase 的 `@source` 指向 workspace 源码；消费方指向 `node_modules/.../dist`。

消费方两种契约（阶段 0 写入包 README，缺一不算阶段 0 完成）：

1. Tailwind：`@source` + `@nocoo/basalt/styles/tailwind`，peer `tailwindcss` ^4。`exports["./styles"]` **指向 tailwind**（与 Kumo 默认一致）。
2. 非 Tailwind：`@nocoo/basalt/styles/standalone`。standalone **不含 Preflight、不含 html/body reset**，只含 tokens + 控件用到的 utility + keyframes。

`data-mode` 与 `.dark` 都认。

**双契约验收（发布门必跑，不能「或」）：**

- Fixture A：干净 Vite + Tailwind + `@source`，不装 standalone。Button 计算色来自 `--basalt-*`。
- Fixture B：干净 Vite **不装 Tailwind**，只 import standalone。Button 同样有宽高和颜色。
- 门 A 与门 B 的 `computed style` 都要断言，不能只断言「有样式」。

### 5.3 构建、exports、依赖

| 项 | 决定 |
|----|------|
| 模块 | ESM only |
| 类型 | 必须产出 `.d.ts`（`vite-plugin-dts` 或等价） |
| `"use client"` | 每个组件 chunk 打 banner |
| `exports["."]` | **仅 stable 叶子 + provider**（不含 charts、不含 DatePicker、不含 catalog） |
| `exports["./components/*"]` | stable + catalog |
| `exports["./providers/*"]` | ThemeProvider、LinkProvider |
| `exports["./charts/*"]` | 仅图表 |
| `exports["./styles"]` | = `styles/tailwind` |
| `exports["./styles/tailwind"]` `./styles/standalone` | 见 5.2 |
| `files` | `dist`、`README`、`LICENSE`、`CHANGELOG`、`NOTICE`（若复制了 Kumo 逻辑） |
| `sideEffects` | 仅 `*.css` |
| `publishConfig.access` | public（第 13 节确认 org） |
| 许可证 | MIT（与本仓库一致）。复制 Kumo 的文件保留 Cloudflare MIT 于 NOTICE |
| `prepublishOnly` | build + 包内 jsdom 测试 + **browser 测试** + publint + **仓外 tarball 门 A/B/C/D**。必须作为 `packages/basalt/package.json` 的 npm lifecycle 写入仓库（02 Wave 8 独立 commit），禁止只写在文档里口头跑 |

**依赖归属（不再写「或」）：**

| 包 | 归属 |
|----|------|
| `react` `react-dom` | peer `^19`（与本仓库 React 19.2 对齐）。**不**宣称 18。门 C 只用 `fixtures/next19` 复制到仓外 tmp |
| `lucide-react` | peer |
| `tailwindcss` | optional peer ^4 |
| `@radix-ui/*` `class-variance-authority` `clsx` `tailwind-merge` `cmdk` `sonner` | **dependencies，不 bundle** |
| `recharts` | optional peer；**禁止**出现在根 barrel |
| `react-day-picker` | optional peer；DatePicker **只**从 `./components/date-picker` 导出，禁止进根 barrel |
| `@tanstack/react-table` | 不进包 |
| `i18next` | 不进包 |

catalog：**发布、随同一 semver**，只走 granular，不进根 barrel。catalog 的 6.2 `wire` **就是** `/ui/:name`，不要求现页已有实例。stable / chart / provider 的 `wire` 必须是**现在就能打开的组合页**（`/` `/health` `/network` 等，不是尚未创建的 `/ui/:name`）。破变更仍走 major。

**测试：** `packages/basalt/**/*.test.tsx` 必须跑。browser 测试（Dialog 焦点、Combobox、DatePicker、Sidebar、CommandPalette）列入 `prepublishOnly` 与阶段 8。

**发布门（仓外临时目录，不能用仓库内 fixture 向上解析根 node_modules）：**

```
tmp=$(mktemp -d)
# 把 tarball 装进去，不要装 recharts、react-day-picker
# A/B/C 全部使用：import { Button, ThemeProvider, ThemeToggle, Toast, LinkProvider } from "@nocoo/basalt"
#   （根 barrel）。以此证明未装 optional peer 时根入口不拉 charts/DatePicker。
# 门 A 额外 smoke：
#   import { Button } from "@nocoo/basalt/components/button"
#   import { ThemeProvider } from "@nocoo/basalt/providers/theme"
# A: Vite+Tailwind+@source
# B: Vite+standalone、无 tailwind
# C: Next 19：hydrate 上述根导入组件，零 hydration warning
# 门 D（另一次仓外 tmp）：安装 recharts 与 react-day-picker 后
#   import { DonutChart } from "@nocoo/basalt/charts/donut"
#   import { DatePicker } from "@nocoo/basalt/components/date-picker"
#   断言模块可加载（不要求画图）。未装 optional peer 时这两条 import 必须失败或跳过，不得从根 barrel 误导出。
# A/B/C/D 都禁止 workspace alias
```

失败不准发。

---

## 6. 两张表：覆盖映射 + 最终公开出口

禁止再把 merge/exclude 和最终导出写在同一行。批准后 **6.2 冻结**；增删走 changelog。

### 6.1 覆盖映射（Kumo / 本站文件 → 我们的出口或丢弃）

对照 Kumo barrel `src/index.ts` @ `1159868dfe32`，以及本站 `src/components/ui/*`、`dashboard/*`。

| 来源 | 处置 | 落到 6.2 的出口 |
|------|------|-----------------|
| Kumo RefreshButton | merge | Button（loading / 图标槽） |
| Kumo DateRangePicker | merge | DatePicker `mode=range` |
| Kumo TimeseriesChart / Chart / ChartLegend | merge | LineChart、AreaChart、Sparkline、ChartPalette |
| Kumo Surface | merge | LayerCard |
| Kumo CloudflareLogo / PoweredByCloudflare | merge | BasaltMark |
| Kumo BubbleMap / ChoroplethMap | exclude | — |
| Kumo DeleteResource | block | 留 app，不导出 |
| 本站 `ui/toast.tsx` | 删除 | Toast（sonner） |
| 本站 `ui/progress.tsx` | merge | Meter（不另导出 Progress） |
| 本站 `ui/card.tsx` | merge | LayerCard |
| 本站 `ui/command.tsx` | merge | CommandPalette |
| 本站 `ui/sonner.tsx` | merge | Toast |
| Pika DataTable | block | 不导出 |
| ActionGridCard | block | 不导出 |
| SummaryMetricCard 写死金额 | 不进包 | StatCard |
| `SecondaryMetricCard.tsx` | merge | StatCard |
| `StackedAreaCard.tsx` | merge | AreaChart（stacked variant） |
| `RadialProgressCard.tsx` | merge | Gauge |
| `MiniDonutCard.tsx` / `PieChartWidget.tsx` | merge | DonutChart |
| `TrendLineCard.tsx` / `MultiLineCard.tsx` / `LineChartWidget.tsx` | merge | LineChart |
| `RecentListCard.tsx` | merge | ItemList |
| `HeatmapCard.tsx` | merge | HeatmapCalendar |
| `BarChartCard.tsx` / `BarChartWidget.tsx` | merge | BarChart |
| `AreaChartCard.tsx` | merge | AreaChart |
| `DonutChartCard.tsx` | merge | DonutChart |
| `GaugeCard.tsx` | merge | Gauge |
| `ItemListCard.tsx` | merge | ItemList |
| `SankeyCard.tsx` | merge | SankeyChart |
| `SparklineCard.tsx` | merge | Sparkline |
| `StackedBarCard.tsx` | merge | StackedBarChart |
| `GroupedBarCard.tsx` | merge | GroupedBarChart |
| `BulletChartCard.tsx` | merge | BulletChart |
| `FunnelChartCard.tsx` | merge | FunnelChart |
| `RadarChartCard.tsx` | merge | RadarChart |
| `TimelineWidget.tsx` | merge | Timeline |
| `DateNavigationWidget.tsx` | merge | DateNavigation |
| `SlotBarChart.tsx` | merge | SlotBarChart |
| `StatCardWidget.tsx` | merge | StatCard |
| PageIntro / LanguageToggle / DashboardLayout 整壳 | block / app | Sidebar 零件可导出 |
| `cn()` | 内部工具 | `packages/basalt/src/utils/cn.ts`，**不**进 6.2、**不**进根 barrel |

### 6.2 最终公开出口（一名一行）

列：`name` | `kind` | `path` | `root` | `stage` | `wire` | `used`

- `kind`：`stable` 进根 barrel；`catalog` 只 granular；`chart` 只 `./charts/*`；`provider` 进根 barrel
- `root`：是否出现在 `exports["."]`
- `wire`：该控件完成时必须接到的 **真实 URL path**（showcase 路由，以 `/` 开头）。布局级控件用 `/`（`DashboardLayout` 包着的任意已有页）。catalog 用 `/ui/:name`。禁止填组件名、header、AppSidebar 等非路由。

| name | kind | path | root | stage | wire | used |
|------|------|------|------|-------|------|------|
| Button | stable | `./components/button` | yes | 1 | `/interactive` | yes |
| LinkButton | catalog | `./components/button` | no | 1 | `/ui/link-button` | no |
| Badge | stable | `./components/badge` | yes | 3 | `/data` | yes |
| Banner | catalog | `./components/banner` | no | 3 | `/ui/banner` | no |
| Breadcrumbs | catalog | `./components/breadcrumbs` | no | 5 | `/ui/breadcrumbs` | no |
| Text | catalog | `./components/text` | no | 1 | `/ui/text` | no |
| LayerCard | stable | `./components/layer-card` | yes | 1 | `/` | yes |
| Empty | catalog | `./components/empty` | no | 3 | `/ui/empty` | no |
| Loader | catalog | `./components/loader` | no | 3 | `/ui/loader` | no |
| SkeletonLine | catalog | `./components/skeleton-line` | no | 3 | `/ui/skeleton-line` | no |
| Meter | stable | `./components/meter` | yes | 3 | `/interactive` | yes |
| ClipboardText | catalog | `./components/clipboard-text` | no | 3 | `/ui/clipboard-text` | no |
| Code | catalog | `./components/code` | no | 3 | `/ui/code` | no |
| CodeBlock | catalog | `./components/code` | no | 3 | `/ui/code-block` | no |
| Label | stable | `./components/label` | yes | 1 | `/forms` | yes |
| Field | catalog | `./components/field` | no | 2 | `/ui/field` | no |
| Input | stable | `./components/input` | yes | 2 | `/forms` | yes |
| InputArea | catalog | `./components/input-area` | no | 2 | `/ui/input-area` | no |
| InputGroup | catalog | `./components/input-group` | no | 2 | `/ui/input-group` | no |
| SensitiveInput | catalog | `./components/sensitive-input` | no | 2 | `/ui/sensitive-input` | no |
| Checkbox | stable | `./components/checkbox` | yes | 2 | `/forms` | yes |
| Radio | catalog | `./components/radio` | no | 2 | `/ui/radio` | no |
| Switch | stable | `./components/switch` | yes | 2 | `/forms` | yes |
| Select | catalog | `./components/select` | no | 2 | `/ui/select` | no |
| Combobox | catalog | `./components/combobox` | no | 2 | `/ui/combobox` | no |
| Autocomplete | catalog | `./components/autocomplete` | no | 2 | `/ui/autocomplete` | no |
| DatePicker | catalog | `./components/date-picker` | no | 2 | `/ui/date-picker` | no |
| Tabs | stable | `./components/tabs` | yes | 5 | `/navigation` | yes |
| Table | catalog | `./components/table` | no | 5 | `/ui/table` | no |
| Pagination | catalog | `./components/pagination` | no | 5 | `/ui/pagination` | no |
| Collapsible | stable | `./components/collapsible` | yes | 5 | `/` | yes |
| Dialog | stable | `./components/dialog` | yes | 4 | `/interactive` | yes |
| AlertDialog | stable | `./components/alert-dialog` | yes | 4 | `/interactive` | yes |
| DropdownMenu | stable | `./components/dropdown-menu` | yes | 4 | `/` | yes |
| Popover | stable | `./components/popover` | yes | 4 | `/interactive` | yes |
| Tooltip | stable | `./components/tooltip` | yes | 1 | `/` | yes |
| Toast | stable | `./components/toast` | yes | 3 | `/` | yes |
| CommandPalette | stable | `./components/command-palette` | yes | 4 | `/` | yes |
| Toolbar | catalog | `./components/toolbar` | no | 5 | `/ui/toolbar` | no |
| Grid | catalog | `./components/grid` | no | 5 | `/ui/grid` | no |
| Link | stable | `./components/link` | yes | 1 | `/` | yes |
| Sidebar | stable | `./components/sidebar` | yes | 5 | `/` | yes |
| TableOfContents | catalog | `./components/table-of-contents` | no | 5 | `/ui/table-of-contents` | no |
| Flow | catalog | `./components/flow` | no | 6 | `/ui/flow` | no |
| MenuBar | catalog | `./components/menu-bar` | no | 5 | `/ui/menu-bar` | no |
| BasaltMark | catalog | `./components/basalt-mark` | no | 5 | `/ui/basalt-mark` | yes |
| Accordion | catalog | `./components/accordion` | no | 3 | `/ui/accordion` | no |
| ContextMenu | catalog | `./components/context-menu` | no | 4 | `/ui/context-menu` | no |
| HoverCard | catalog | `./components/hover-card` | no | 4 | `/ui/hover-card` | no |
| NavigationMenu | catalog | `./components/navigation-menu` | no | 5 | `/ui/navigation-menu` | no |
| Slider | catalog | `./components/slider` | no | 2 | `/ui/slider` | no |
| Toggle | catalog | `./components/toggle` | no | 2 | `/ui/toggle` | no |
| ToggleGroup | catalog | `./components/toggle-group` | no | 2 | `/ui/toggle-group` | no |
| Separator | stable | `./components/separator` | yes | 1 | `/forms` | yes |
| Sheet | stable | `./components/sheet` | yes | 4 | `/interactive` | yes |
| Avatar | stable | `./components/avatar` | yes | 3 | `/` | yes |
| ThemeToggle | stable | `./components/theme-toggle` | yes | 1 | `/` | yes |
| ThemeProvider | provider | `./providers/theme` | yes | 1 | `/` | yes |
| LinkProvider | provider | `./providers/link` | yes | 1 | `/` | 新 |
| StatCard | chart | `./charts/stat-card` | no | 6 | `/health` | yes |
| SlotBarChart | chart | `./charts/slot-bar` | no | 6 | `/health` | yes |
| BarChart | chart | `./charts/bar` | no | 6 | `/` | yes |
| LineChart | chart | `./charts/line` | no | 6 | `/health` | yes |
| AreaChart | chart | `./charts/area` | no | 6 | `/` | yes |
| DonutChart | chart | `./charts/donut` | no | 6 | `/` | yes |
| GroupedBarChart | chart | `./charts/grouped-bar` | no | 6 | `/` | yes |
| StackedBarChart | chart | `./charts/stacked-bar` | no | 6 | `/network` | yes |
| Sparkline | chart | `./charts/sparkline` | no | 6 | `/components` | yes |
| HeatmapCalendar | chart | `./charts/heatmap-calendar` | no | 6 | `/health` | yes |
| Gauge | chart | `./charts/gauge` | no | 6 | `/` | yes |
| RadarChart | chart | `./charts/radar` | no | 6 | `/network` | yes |
| FunnelChart | chart | `./charts/funnel` | no | 6 | `/components` | yes |
| BulletChart | chart | `./charts/bullet` | no | 6 | `/banking` | yes |
| Timeline | chart | `./charts/timeline` | no | 6 | `/health` | yes |
| SankeyChart | chart | `./charts/sankey` | no | 6 | `/network` | yes |
| ItemList | chart | `./charts/item-list` | no | 6 | `/` | yes |
| DateNavigation | chart | `./charts/date-navigation` | no | 6 | `/health` | yes |
| ChartPalette | chart | `./charts/palette` | no | 6 | `/palette` | yes |

`used=no` 的 catalog：wire 必须是可打开的 `/ui/:name`，**不是** `docs/baselines/`。

**Sidebar 例外：** 表中 `wire=/` 仍是最终必须接到的现壳。实现阶段只填 catalog `/ui/sidebar` 并做 browser 测试；**禁止**在阶段 5 把 `AppSidebar` 换成包。现页 `/` 替换在阶段 7 S2 完成。其它 stable/chart/provider 仍是「做完即接到表内现页」。

图表均要 `aria-label` 或 `summary`。`palette.ts` 迁到 `./charts/palette`，showcase 再转 export，颜色不得变。

不导出：Progress、RefreshButton、Surface、DateRangePicker、Maps、DataTable、ActionGrid。

---

## 7. 底稿流程

### 7.1 步骤（可复现）

钉死的来源 SHA（实施时若要换底稿，先改本文再动手）：

| 仓 | SHA |
|----|-----|
| Kumo | `1159868dfe32` |
| meowth | `bb02d5a18e00` |
| pew | `97a890fabe6e` |
| pika | `d9b12caf26a4` |
| surety | `0ac675823a18` |
| otter | `10f4b2d4d137` |
| signoff.now | `92033c89d807` |
| gecko | `afa765ea4280` |
| zhe | `c31c239f01c9` |
| intentional-kusto-queries（Whiteboard） | `bce8a88fe26e` |
| 本站审查基线 | `2727ae6a8d3f` |

打分两张表，禁止混成一张：

- **行为/API（0–3）：** props 完整、compound、a11y、有测试、SSR 安全。硬门：模块顶层碰 `window` 直接淘汰。
- **视觉（0–3）：** 能否用 className/variant 在本站基线复现。硬门：会改现页默认像素且无法用 className 压回去 → 淘汰。
- 平局：本站现实现优先；再按测试覆盖。
- 同族一起定：Field 族；Link/Sidebar；Toast/Theme；图表 palette。

赢家写入 `/ui/:name`「来源」：上表 SHA + 文件路径。catalog 的 wire 是 `/ui/:name` 页面，不是截图目录。

### 7.2 家族比选（API 底稿；视觉仍本站）

摸底排除 `manifest` `multica` `eagle-webui` `workany`。

新 clone：20 仓中 L0 11、Gen 2 app-shell 8、两者都有 7（otter pika ellie noheir signoff.now steed arena）。

**硬约束：** 底稿只提供 API / a11y / 测试。默认 size、边框、浮岛 class 跟 **本站该实例**。meowth `h-10` 只能作为非默认 `size` variant 进包（给家族迁移），不得改本站默认。meowth 三文件壳禁止落地本站。给无边卡统一加 ring 禁止。

本站壳已经是内容区浮岛。7.2 里「不能把本站壳换成 rounded-island」是指 **不能换成 meowth AppShell 结构**，不是拆掉现有 `rounded-[16/20px] bg-card`。

| 控件 | API 底稿 | 不要当底稿 |
|------|----------|------------|
| Button | meowth@`bb02d5a18e00` `apps/dashboard/src/components/ui/button.tsx`；default 改回本站 `h-9` | 本站无 Slot |
| Input | meowth@`bb02d5a18e00`；**现页默认保持 `bg-background`** | 把现页 Input 全改成 `bg-secondary` |
| Field | signoff.now@`92033c89d807` `apps/web/src/components/Field.tsx` | FormsPage 手拼 |
| Select | meowth@`bb02d5a18e00` / surety@`0ac675823a18` | pika 原生 select |
| Dialog | meowth Confirm API；**现页边框/overlay 跟本站** | 把现页 Dialog 改成另一套 overlay |
| Tabs | otter@`10f4b2d4d137` `packages/web/src/components/ui/tabs.tsx` | pew PeriodTabs |
| Table primitive | surety@`0ac675823a18` 轻量 table | 把 Pika DataTable 当 2.0 稳定 API |
| Switch | meowth | |
| Checkbox | pika（indeterminate） | |
| Badge | meowth/surety | |
| Tooltip | meowth API；现页不要强行 Arrow | |
| Toast | **本站 sonner** + Provider | 旧 toast.tsx |
| Empty | surety/meowth empty-state | |
| Skeleton | meowth | |
| StatCard | pew API | meowth ring；写死金额的 Summary |
| LayerCard | pika 无边是 **variant**，不是唯一默认若现页有边 | 本站 Card 默认 border 直接当唯一皮肤 |
| CommandPalette | surety | 绑死 AppSidebar 的 cmdk 皮肤 |
| Pagination | pika pagination 的受控 API，可抽离 DataTable | |
| DatePicker | gecko calendar | |
| ThemeToggle | 本站三态 + pew `useSyncExternalStore` | 模块顶层读 localStorage |
| 本站壳 | DashboardLayout + AppSidebar | frogie/dotty 稀释拷贝；meowth 三文件 |
| SlotBarChart | 本站（有测试） | 并进 BarChart |
| DonutChart | 本站视觉 + 新 data props | 写死分类的 Card |

DataTable、ActionGrid：block，不进 6.1 stable。

### 7.3 i18n / SSR

进包控件不调用 `useTranslation()`。不在模块顶层碰 `window` / `localStorage` / `document`。Theme 与 Toast 经 Provider；门 C 必须 hydrate ThemeProvider + ThemeToggle + Toast + LinkProvider + Button，零 hydration warning。

DatePicker / DateNavigation：`locale`、`weekStartsOn`、`timeZone`、`formatDate` 为 props，默认与本站现行为一致。

---

## 8. 展示站 IA

现有 `NAV_GROUPS` 不重组。新增折叠组「控件库」→ 分类索引 → `/ui/:name`。现有「控件」组（`nav.controls`）名称与条目不动。⌘K 索引文档。

文档页最低：预览、import、可复制用法（真写 clipboard）、variants、props、来源（SHA）。

`/components` 组合墙：阶段 7 再改内部 import，此前原样。

---

## 9. 阶段

禁止估时。提交粒度、Wave B 先于 Wave P、placeholder 先行、先小后大、图表单独轨道、发布提交见 **02**。下面只保留与 6.2 的对应关系。

### 阶段 0

前提：02 Wave B 已完成 B0 播种与 B1 截图（在 Wave P 改侧栏之前）。阶段 0 不再补拍「改导航前」的基线。

- bun workspace `packages/basalt`
- `--basalt-*` tokens + showcase 映射；现页颜色不变
- CSS 双契约写进包 README（阶段 0 完成条件，对应 02 Wave 0.6，**不得**拖到 Wave 8）
- 包构建产出 `.d.ts`，组件 chunk 打 `"use client"` banner（02 Wave 0.7）
- **先**让 vitest/tsc 覆盖 `packages/basalt`（可空包），**再**往包里写 tokens，避免 husky 看不到包内错误
- 空 exports 骨架；仓内 `fixtures/vite-tailwind`、`vite-standalone`、`next19` 仅作模板，发布门复制到仓外 tmp
- **验收：** 本站所有已有路由与基线截图一致（侧栏允许多一个默认折叠的「控件库」组）

### 阶段 1 地基

与 6.2 `stage=1` 对齐：Text LayerCard Button LinkButton Link Tooltip Label Separator ThemeProvider ThemeToggle LinkProvider。`cn()` 是内部工具（6.1），同阶段实现但不公开导出。

每项：测试 + `/ui/:name`。其中 6.2 为 stable/provider 的，再接到表里的现页 wire（Button → `/interactive`；Label → `/forms`；Separator → `/forms`；ThemeProvider / ThemeToggle / Link / LinkProvider / Tooltip / LayerCard → `/`）。LinkButton 是 catalog，只接 `/ui/link-button`。

### 阶段 2 表单

与 6.2 `stage=2` 对齐：Input InputArea InputGroup SensitiveInput Checkbox Radio Switch Select Slider Field Combobox Autocomplete DatePicker Toggle ToggleGroup

Combobox、DatePicker：本阶段做 browser 测试。

### 阶段 3 反馈

与 6.2 `stage=3` 对齐：Badge Banner Empty Loader SkeletonLine Meter Toast ClipboardText Code CodeBlock Avatar Accordion

### 阶段 4 浮层

与 6.2 `stage=4` 对齐：Dialog AlertDialog Popover DropdownMenu ContextMenu HoverCard Sheet CommandPalette

Dialog、CommandPalette：browser 测试。

### 阶段 5 导航结构

与 6.2 `stage=5` 对齐：Tabs Collapsible Pagination Breadcrumbs NavigationMenu MenuBar Toolbar Table TableOfContents Grid Sidebar BasaltMark

Sidebar：browser 测试。catalog 填 `/ui/sidebar`。现页 `wire=/` 推迟到阶段 7 S2，见 6.2 注。mobile drawer 所有权在 Layout。

### 阶段 6 图表

与 6.2 `kind=chart` + Flow 对齐。先 StatCard SlotBarChart BarChart DonutChart LineChart AreaChart SankeyChart ChartPalette。Flow catalog 放末。无 Maps。

palette 迁移不得改变现图颜色。

### 阶段 7 拼壳

`DashboardLayout` `AppSidebar` 其余组合页。只替换实现。Sidebar 的现页 `/` wire 在 S2 完成。登录等仍无侧栏。对照冻结截图。删除已迁走的 app `ui/` 死代码。前置：6.2 全部 catalog 已填满，且 stable / chart / **provider** 均已接到表内 wire（Sidebar 除外，见上）。

### 阶段 8 发布

publint、仓外 tarball 门 A/B/C/D、typecheck、CHANGELOG、NOTICE（若复制 Kumo）。先 `2.0.0-alpha.n`，自吃后再 `2.0.0`。原子提交清单见 02 §8.9，禁止只在口头跑门。

---

## 10. 提交

一逻辑一 commit。一控件一 `feat:`。阶段 0 基础设施可拆多次 `chore:` / `feat:`。阶段 7 一页一 `refactor:`。禁止 `git add -A`。

---

## 11. 6DQ

| 维 | 要求 |
|----|------|
| L1 | 每 stable/catalog 控件：默认渲染、主 variant、disabled、icon-only 名称。图表：给定 data 出对应图形。覆盖 packages/basalt |
| L2 | exports 与文件一致；仓外 tarball：**Tailwind+@source** 与 **standalone** 两条都断言 Button 的宽高和 `--basalt-*` 颜色 |
| L3 | 2.0 不做全站 Playwright 全家桶。阶段 0/7 用冻结截图对照。browser 测试进 prepublishOnly |
| G1 | biome + typecheck（含 packages/basalt） |
| G2 | osv-scanner；**不把密钥写进包**。CSS tokens 必须随包 |
| 文档 | 每控件一页；改 API 改本文 manifest |

---

## 12. 本站 vs 家族后续（能力矩阵，不是一代标签）

| 能力 | 本站 2.0 包 | 家族 Gen 2（surety/meowth） | 2.0 是否对齐 |
|------|-------------|------------------------------|--------------|
| 内容区浮岛 | 已有 `rounded-[16/20px] bg-card` | `rounded-island` + 三文件壳 | 保留本站岛；不换三文件 |
| 侧栏结构 | 单体 AppSidebar | sidebar + context | 零件进包；整壳 showcase |
| 移动抽屉 | Layout 里 overlay | AppShell 的 Sheet + 焦点归还 | 所有权在 Layout，可学焦点归还，不改现视觉 |
| 亮度 | 注释 L0–L2 | L0–L3 | 不把现页改成四层 |
| 密度 | Button 默认 `h-9` | 常 `h-10` | 默认冻本站；包提供非默认 `size` 含 h-10，2.0 就冻结这套 variant，不留给迁移时再改公共 API |
| L3 表面 | 现页实例有边/无边都冻 | L3 常见 ring/border | LayerCard 用 variant 表达有边/无边；默认不改现页 |
| Header 槽 | 标题 + GitHub + i18n + theme | breadcrumbs / palette / DbSelector | 不抄产品槽 |
| Token 名 | 包内 `--basalt-*` | 各仓 shadcn 名 | 避免覆盖消费方 |

其它仓替换是 2.0 **之后**。他们若要 h-10 / L3，用 variant 或 2.x，不在本站默认真改。

Sidebar 扩展边界（阶段 5 前写进控件文档）：受控 `collapsed`、`Link` renderer、header/footer/user slots、mobile 是否由外部 Sheet 控制。

---

## 13. 必须先拍板（不够这张表不准阶段 0）

1. 视觉基线：B0 播种后的 HEAD + 第 3.2 截图矩阵。冻实例现状，不统一无边 L2？允许 B0 只改随机序列。
2. 侧栏在现有「控件」组（`nav.controls`）之外，增加折叠的「控件库」索引组（`nav.kit`）？现有组名与路由不动。
3. CSS：`--basalt-*` 变量 **且** `bg-basalt-*` utility；`./styles` = tailwind；standalone 无 Preflight；两条契约分开验收？
4. 6.2 最终出口表作为冻结清单？catalog 发布、只 granular、同一 semver？
5. Maps exclude；RefreshButton/Surface/DateRangePicker merge；AlertDialog 独立；DataTable/ActionGrid block？
6. Radix **dependencies、不 bundle**；clsx / tailwind-merge 同为 dependencies？
7. DatePicker / 图表不进根 barrel？
8. 仓外 tmp tarball 门（不装 optional peer）+ Next hydrate ThemeProvider/ThemeToggle/Toast/LinkProvider？
9. npm public `@nocoo/basalt`？许可证 MIT + NOTICE？
10. 先 `2.0.0-alpha`？计划已定为 React peer **只 19**、一门 Next（`next19`）。若要改回 18 需先改本文再开工。
11. 移动抽屉所有权在 Layout？密度 `size`（含 h-10 非默认）在 2.0 冻结？

未勾完不准阶段 0。

---

## 14. 2.0 明确不写

- 工时
- 其它仓 PR
- 照搬 Kumo Figma / changeset / screenshot worker
- 把本站改成 meowth 三文件壳

---

## 15. 锚点

| 用途 | 路径 |
|------|------|
| 本站 token | `src/index.css` |
| 本站浮岛 | `src/components/DashboardLayout.tsx`（内容区 `rounded-[16px] md:rounded-[20px] bg-card`） |
| 本站侧栏 | `src/components/AppSidebar.tsx` |
| 有边卡反例 | `src/components/dashboard/BulletChartCard.tsx` |
| 无边卡 | `src/components/dashboard/ActionGridCard.tsx` |
| 写死数据 | `src/components/dashboard/DonutChartCard.tsx` |
| 较好图表 API | `src/components/dashboard/SlotBarChart.tsx` |
| Input 默认表面 | `src/components/ui/input.tsx` |
| Vitest 不含 packages | `vitest.config.ts` `include` |
| Kumo 样式契约 | `/Users/nocoo/workspace/references/kumo/packages/kumo/README.md` Usage / `@source` |
| Kumo barrel | `/Users/nocoo/workspace/references/kumo/packages/kumo/src/index.ts` @ `1159868dfe32` |
| 家族密度 | `/Users/nocoo/workspace/personal/zhe/docs/22-design-tokens.md` |

Wave B / Wave P / 阶段 0 只在第 13 节全部拍板且 01+02 联审 Sign Off 之后开始。
